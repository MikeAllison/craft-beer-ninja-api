Frontend:

- Checks for browser GeoLocation API:
-- Submits lat/lng to api/reverse-geocode to get city, state
-- Submits lat/lng to api/places
-- Submits lat/lng and place lat/lng to api/calulate-distance
-- Stores city, state, lat/lng, place count in LocalStorage

- Submits form to api/geolocate to get lat/lng
-- Submits lat/lng to api/places
-- Stores city, state, lat/lng, place count in LocalStorage
